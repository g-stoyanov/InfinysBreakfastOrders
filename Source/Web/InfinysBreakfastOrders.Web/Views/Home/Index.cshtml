@model IEnumerable<InfinysBreakfastOrders.Web.ViewModels.Home.IndexOrderViewModel>

    @{
        ViewBag.Title = "Home Page";
    }

    <div class="container">
        @foreach (var order in Model)
        {
            <div class="row">
                <div class="col-md-2">
                    <h3>username</h3>
                </div>
                <div class="col-md-10">
                    <h4>@order.OrderText</h4>
                </div>
            </div>
            <hr />
        }
    </div>